import { makeStyles } from "@material-ui/core/styles";
export const useStyles = makeStyles({
  genderLabel: {
    "& .MuiFormLabel-root": {
      "&.Mui-focused": {
        color: "#55e8d5"
      }
    }
  }
});
